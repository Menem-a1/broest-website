# 📋 خطة مراجعة وتحسين موقع BROEST — للتنفيذ عبر جلسة Claude عادية (chat)

**ملاحظة مهمة قبل البدء:** هذه الخطة موجّهة لجلسة Claude عادية في واجهة الشات
(وليس Claude Code)، والفرق العملي الوحيد هو طريقة الوصول للملفات.

---

## المرحلة -1 — تجهيز الملفات (خاصة بجلسة الشات العادية)

1. اضغط المشروع بالكامل في ملف واحد:
   ```bash
   zip -r broest-project.zip . -x "node_modules/*" -x ".git/*" -x "dist/*"
   ```
2. ارفع الملف المضغوط في بداية المحادثة الجديدة مع Claude.
3. اطلب من Claude أن يفك الضغط أولًا في بيئة العمل الخاصة به قبل تنفيذ أي
   خطوة من المراحل التالية.
4. تأكد أن Claude يخبرك صراحة أنه فك الضغط ورأى قائمة الملفات فعليًا (وليس
   افتراضًا) قبل المتابعة — اطلب منه أن يعرض لك `ls` أو ما يعادلها كدليل.

**ملاحظة سياقية:** فحص أولي مسبق على نفس المشروع (`pnpm install --frozen-lockfile`
ثم `tsc -b` ثم `vite build`) أعطى نتيجة: **صفر أخطاء تمامًا**. الأخطاء المذكورة
أصلًا في وصف المهمة (`Cannot find module`, JSX errors) كانت بسبب غياب
`node_modules` فقط، وليست أخطاء حقيقية في الكود. الخطة مبنية على هذا الأساس،
لكن يجب إعادة التحقق من هذا في المرحلة 0 لأن حالة المشروع قد تكون تغيّرت.

---

## المرحلة 0 — التحقق (Verification)

```bash
corepack enable
corepack pnpm install --frozen-lockfile
./node_modules/.bin/tsc -b
./node_modules/.bin/vite build
```

**معيار القبول:** لو الأربع أوامر نجحوا بدون أخطاء، تُقفل هذه المرحلة فورًا
وينتقل مباشرة للمرحلة 1 بدون أي تعديل كود. لا تُعتبر أي أخطاء ناتجة عن نقص
في الحزم (module not found) أخطاء حقيقية — فقط أخطاء تظهر بعد تثبيت كامل
للحزم تُعتبر حقيقية.

---

## المرحلة 1 — الفحص فقط (بدون أي تعديل) — تقرير أول

Claude يفحص المشروع ويقدّم تقريرًا **بدون تعديل حرف واحد**، مقسّمًا لستة أقسام:

1. **الأمان:** أسرار مكشوفة، استخدام `dangerouslySetInnerHTML`/`eval`، إعدادات
   CORS، فحص صلاحيات الوصول (auth checks) على المسارات المحمية.
2. **التبعيات:** تشغيل `pnpm outdated` و`pnpm audit` — تصنيف كل ثغرة:
   مباشرة (تؤثر فعليًا على الموقع الحي) أو غير مباشرة (أدوات تطوير داخلية فقط).
3. **كود مكرر:** أي منطق متكرر بشكل حقيقي في أكثر من ملف (تنسيق بيانات،
   تحقق من صحة مدخلات، نداءات API متشابهة).
4. **دوال/ملفات طويلة جدًا أو متعددة المسؤوليات:** تحديد فقط بدون تنفيذ أي
   إعادة هيكلة في هذه المرحلة.
5. **معالجة الأخطاء:** أي نداء لقاعدة البيانات (Supabase) بدون `try/catch`،
   أي عملية شبكة بدون حالة تحميل/خطأ واضحة تظهر للمستخدم.
6. **الأداء:** أي استعلام لقائمة بيانات كبيرة بدون `.range()` (pagination)،
   أي أصول (assets) كبيرة الحجم بشكل غير ضروري.

**الشكل المطلوب للتقرير:** جدول لكل قسم يتضمن: المشكلة، اسم الملف والسطر
التقريبي، سبب الأهمية، الحل المقترح. **بدون أي كود تعديل فعلي في هذه المرحلة.**

**قاعدة صارمة:** يُمنع الانتقال للمرحلة 2 قبل أن يطّلع المستخدم على هذا
التقرير ويوافق صراحة على كل بند سيتم تنفيذه.

---

## المرحلة 2 — التعديلات الأمنية فقط (بعد الموافقة الصريحة)

ترتيب التنفيذ: **الأخطر أولًا**. لكل تعديل:
- وصف واضح لأي تغيير سلوك متعمد (مثال: "الطلب سيُرفض إذا كان رقم التليفون
  غير صالح، وهذا تغيير سلوك مقصود وليس عيبًا").
- تشغيل `tsc -b` و`vite build` فورًا بعد كل تعديل، والتوقف الفوري عند أي فشل.
- عدم الانتقال لتعديل تالٍ قبل التأكد من نجاح السابق فعليًا (وليس افتراضًا).

---

## المرحلة 3 — تحديث التبعيات (بعد إتمام المرحلة 2)

- ترقية إصدارات **patch/minor** بحرية، مع تحديث `pnpm-lock.yaml`.
- أي ترقية **major version** (تُذكر فقط باسمها وسبب عدم التنفيذ في تقرير
  منفصل، مع جملة توضيحية واحدة عن التوافق) — **بدون تنفيذ فعلي في هذه المرحلة**.
- تشغيل `tsc -b` و`vite build` بعد كل دفعة تحديث.

---

## المرحلة 4 — التنظيف الآمن (Safe Cleanups)

**فقط بعد موافقة صريحة منفصلة على كل بند:**
- استخراج الكود المكرر (إن وُجد فعليًا) إلى دالة/هوك مشترك — يجب ألا يتغير
  أي سلوك ظاهر للمستخدم النهائي.
- تقسيم الملفات الطويلة جدًا (إن تمت الموافقة) — تقسيم بنائي فقط، بدون أي
  تغيير في المنطق الداخلي.
- لكل تعديل: عرض قبل/بعد، مع التأكيد الصريح أنه لا يوجد تغيير في السلوك.

---

## المرحلة 5 — التقرير النهائي (إلزامي)

يجب أن يتضمن حصريًا:
1. **قائمة الملفات المعدَّلة** (بالاسم الكامل، وسبب كل تعديل في جملة واحدة).
2. **قائمة الملفات التي لم تُمس إطلاقًا.**
3. **نتيجة آخر فحص build/tsc فعلي** (نسخ حرفي لناتج الطرفية، وليس وصفًا عامًا).
4. **الثغرات التي أُصلحت** (بالاسم أو رقم CVE إن وُجد).
5. **الترقيات الكبيرة المؤجلة** (إن وُجدت) مع سبب التأجيل.
6. **صراحة كاملة إلزامية:** ممنوع استخدام عبارات مثل "100% جاهز" أو "تم كل
   شيء". الصيغة الصحيحة: "تم فحص X وبناؤه بنجاح، ولم يتم اختبار Y يدويًا
   (لعدم وجود اختبارات آلية في المشروع)".

---

## ملاحظة أخيرة

هذا المشروع **لا يحتوي على اختبارات آلية (tests)** بشكل أساسي، لذا أي "فحص"
في هذه الخطة يعني بناء حقيقي (`build`) فعلي + مراجعة يدوية للمنطق، وليس
تشغيل مجموعة اختبارات جاهزة. إذا رغبت أن تكتب Claude اختبارات أساسية كجزء
من هذه المهمة، هذا يستحق أن يكون **مرحلة 6 منفصلة واختيارية**، حتى لا يختلط
مع باقي المراحل الأساسية أعلاه.
