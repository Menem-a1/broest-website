# نقطة الرجوع الآمنة — قبل ترقية react-router-dom إلى v7

**تاريخ إنشاء النسخة:** بيانات آخر commit مستقر قبل بدء الترقية.

## الكود
- **آخر commit مستقر:** `67c3504` — "fix: order counter timezone bug + admin-only favicon"
- **علامة Git محلية:** `pre-router-v7-upgrade` (موجودة في نسخة الكود المحلية، لم تُرفع بعد لـ GitHub لعدم توفر بيانات الدخول في بيئة التنفيذ هذه)
- **الملف المرفق:** `broest-backup-before-router-v7.zip` — نسخة كاملة من كل ملفات المشروع في هذه اللحظة بالضبط

## قاعدة البيانات (مرجعية فقط، لم تتأثر بالترقية)
- عدد الطلبات وقت أخذ النسخة: 80 طلب
- آخر طلب مسجل: 2026-09-05 11:15:49 UTC
- الترقية هذه تخص الكود فقط (مكتبة التنقل)، ولا تلمس قاعدة البيانات أو أي بيانات عملاء إطلاقًا

## كيفية الاستعادة لو حصلت مشكلة بعد رفع الترقية

### لو الترقية لسه في نسخة تجريبية (website-demo) ولم تُرفع لبروست الحي بعد:
لا حاجة لأي استعادة — فقط لا تُكمل نشر التحديث على مشروع بروست الحي، واستمر باستخدام الكود الحالي بدون تغيير.

### لو تم رفع الترقية بالفعل على بروست الحي وظهرت مشكلة:
1. فك ضغط الملف المرفق `broest-backup-before-router-v7.zip` في مكان جديد
2. ارفع محتوى هذا الملف بالكامل على GitHub (استبدال الملفات الحالية)
   — تأكد فقط من استبدال `package.json` و `pnpm-lock.yaml` و مجلد `src` بالكامل
3. Vercel سيعيد النشر تلقائيًا بمجرد رفع الكود القديم على GitHub (لأن الربط تلقائي)
4. لا حاجة لأي تعديل على قاعدة بيانات Supabase — لم تتأثر بهذه الترقية إطلاقًا

## الحالة الحالية للاختبار (وقت كتابة هذا الملف)
- الترقية تمت في نسخة تجريبية منفصلة تمامًا (`/home/claude/router_test`)، بروست الحي لم يُلمس بعد
- نتائج الفحص التقني (`tsc -b`, `vite build`): ناجحة بالكامل، صفر أخطاء
- لم يتم بعد: اختبار تفاعلي حقيقي (فتح الموقع والضغط على كل جزء) بسبب قيود شبكة في بيئة التنفيذ
- الخطوة التالية المتفق عليها: نشر النسخة المرقّاة على مشروع `website-demo` (التجريبي) على Vercel، وليس بروست الحي مباشرة، لتجربتها يدويًا قبل أي قرار نهائي
